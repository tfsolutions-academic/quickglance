{
  "routes": [
    {
      "method": "GET",
      "path": "/staff-categories",
      "handler": "staff-category.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/staff-categories/count",
      "handler": "staff-category.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/staff-categories/:id",
      "handler": "staff-category.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/staff-categories",
      "handler": "staff-category.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/staff-categories/:id",
      "handler": "staff-category.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/staff-categories/:id",
      "handler": "staff-category.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
